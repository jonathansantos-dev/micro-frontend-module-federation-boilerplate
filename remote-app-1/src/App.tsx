import React from 'react';
import AuthModule from './components/AuthModule';

/** Standalone entry — used when running remote-app-1 independently for dev/testing */
const App: React.FC = () => <AuthModule />;

export default App;
